#
# Project Configuration File
#
# A detailed documentation with the EXAMPLES is located here:
# http://docs.platformio.org/en/latest/projectconf.html
#

# A sign `#` at the beginning of the line indicates a comment
# Comment lines are ignored.

[env:nucleo_f303k8]
platform = ststm32
framework = mbed
board = nucleo_f303k8
upload_port = /media/david/NODE_F303K8

# I'll problem want RTOS at some point, but it doesn't link successfully
# at the moment, so leaving it out
# build_flags = -DPIO_FRAMEWORK_MBED_RTOS_PRESENT -std=gnu++14
build_flags = -std=gnu++14

lib_deps = 185

